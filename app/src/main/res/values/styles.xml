<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#34b8f4</item>
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDark</item>-->
        <item name="colorAccent">@color/colorAccent</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">#34b8f4</item>
        <!--Window color-->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">#34b8f4</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
    <style name="ActionButton.Overflow.ZhiHu" parent="android:style/Widget.Holo.Light.ActionButton.Overflow">
        <item name="android:src">@drawable/menu_2</item>
    </style>
    <style name="Theme.ToolBar.ZhiHu" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="actionOverflowButtonStyle">@style/ActionButton.Overflow.ZhiHu</item>
    </style>

    <style name="Theme.menu" parent="@android:style/Theme.Material.Settings">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="MainTabLayout">
        <item name="tabIndicatorColor">@android:color/transparent</item> <!-- tabLayout 底部选择状态横线的的颜色：透明 -->
        <item name="tabIndicatorHeight">0dp</item><!-- tabLayout 底部选择状态横线的高度：0 -->
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
    </style>

</resources>
